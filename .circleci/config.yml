jobs:
  deploy:
    docker:
      - image: cibuilds/base
    steps:
      - attach_workspace:
          at: my-site
      - run: ls -lah my-site
version: 2.1
orbs:
  hugo: circleci/hugo@0.5.2
workflows:
  welcome:
    jobs:
      - hugo/build:
          html-proofer: false
          version: '0.68.3'
      - deploy:
        #  filters:
        #    branches:
        #      only: master
          requires:
            - hugo/build
